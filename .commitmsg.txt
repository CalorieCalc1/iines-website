Fix dead space on shorter pricing cards, add "Best for" self-selection callouts

The Revenue Engine card has 11 feature bullets vs 7-8 on the other
three tiers, but .pricing-features had flex-grow:1 to push the CTA
button to the bottom of the card. That stretched the invisible <ul>
box itself rather than its content, leaving a visible gap between the
last bullet and the button on the shorter cards.

Moved the growth to the button instead (.pricing-card > .btn {
margin-top: auto }) so content sizes naturally and the button still
aligns across all 4 cards in the grid row. Filled the resulting space
with a "Best for" callout on each card, one line of self-selection
copy tying into the existing "what stage is your business at"
framing from the homepage sticky CTA, so it's functional rather than
decorative filler.

Bumped the CSS cache-bust version (v8 -> v9) across all 19 HTML files
that load it.

Co-authored-by: factory-droid[bot] <138933559+factory-droid[bot]@users.noreply.github.com>
